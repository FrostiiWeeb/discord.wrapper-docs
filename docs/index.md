## Getting Started

## Basic bot

```python
from discord import wrapper

bot = wrapper.Bot(token="Your TOKEN")

bot.run()
``` 

=============

`class Bot(token, command_prefix, intents)`

Type:
	`class`
	
=============
	
`def run()`

Type:
	`function`